---
import BaseLayout from "../layouts/BaseLayout.astro";
import "../styles/global.css";
---

<BaseLayout>
  <div class="contact">
    
    <div class="info">
    <p class="section-title">Rufen Sie uns an:</p>
    <p>Hans Muster</p>
    <a href="mailto:nils@grischa-web.ch">kontakt@piz-patty.ch</a>
    <a href="tel:+41799689805">+41 12 345 67 89</a>
    <p>Standort: Chur, Graubünden</p>
    <hr />
  </div>
</div>

  <div class="form-container">
    <div>
      <p class="section-title">Bei Fragen oder Anmerkungen können Sie sich gerne melden</p>
    </div>
    <form action="https://api.web3forms.com/submit" method="POST">
      <input
        type="hidden"
        name="access_key"
        value={import.meta.env.PUBLIC_WEB3FORMS_KEY}
      />
      <label class="title" for="name">Name</label>
      <input type="text" name="name" required />

      <label class="title" for="email">E-Mail</label>
      <input type="email" name="email" required />

      <label class="title" for="message">Ihre Nachricht</label>
      <textarea name="message" required></textarea>
      <button class="secondary-btn" type="submit">Senden</button>

      
      <span>Ich melde mich in der Regel innert 24 Stunden.</span>

      <input type="checkbox" name="botcheck" style="display: none;" />
    </form>
  </div>

  
</BaseLayout>

<style>
  .contact{
    display: flex;
    gap: 1.8rem;
    width: 100%;
    max-width: 1200px;
    justify-content: center;
    margin: 150px auto 30px auto;
  }

  @media (max-width: 769px) {
    .contact {
      flex-direction: column;
      align-items: center;
    }
  }

  .section-title{
    color: var(--color-piz-text-bright);
    font-size: clamp(1.3rem, 5vw, 1.8rem);
    color: var(--color-piz-accent);
  }

  @media (max-width: 769px) {
    section {
      flex-direction: column;
      align-items: center;
    }
  }

  .form-container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    text-align: center;
    width: 100%;
    max-width: 536px;
    margin: 0 auto;
    padding: 2rem;
    background-color: var(--color-piz-card-bg);
    border-radius: 5px;
    box-shadow: 0 5px 10px rgba(0, 0, 0, 0.158);
  }

  label {
    color: var(--color-piz-text-bright);
    font-size: clamp(0.8rem, 4vw, 1rem);
    align-self: flex-start;
    text-transform: uppercase;
  }

  form {
    display: flex;
    flex-direction: column;
    align-items: center;
    font-size: clamp(1rem, 10vw, 2rem);
  }

  button {
    font-size: 1.2rem;
    cursor: pointer;
  }

  textarea {
    height: 200px;
  }

  input,
  textarea {
    margin-bottom: 1.6rem;
    width: 100%;
    max-width: 600px;
    font-size: 1rem;
    color: var(--color-piz-text-bright);
  }

  input{
    border-bottom: 1px solid var(--color-piz-text-mute);
    padding: 0.8rem 0 0.3rem 0.5rem;
  }

  input:focus{
    border-bottom: 1px solid var(--color-piz-accent);
  }

  textarea{
    border: 1px solid var(--color-piz-text-mute);
    border-radius: 15px;
    padding: 0.5rem 1rem;
    margin-top: 0.8rem;
  }

  textarea:focus{
    border: 1px solid var(--color-piz-accent);
    box-shadow: 0px 0px 20px var(--color-piz-accent);
  }

  input:not([type="hidden"]):not([name="botcheck"]):focus,
  textarea:focus {
    outline: none;
    box-shadow: 0px 0px 20px var(--color-piz-shadow);
  }

  input,
  textarea {
    transition: box-shadow 0.3s;
  }

  span{
    font-size: 0.8rem;
    color: var(--color-piz-text-mute);
    margin-top: 1.5rem;
  }

  .info{
    width: 60%;
    text-align: center;
    font-size: clamp(1rem, 2.5vw, 1.5rem);
    padding: 2rem;

    color: var(--color-piz-text-bright);
    background-color: var(--color-piz-card-bg);
    border-radius: 5px;
    box-shadow: 0 5px 10px rgba(0, 0, 0, 0.158);

    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  hr{
    display: none;
    margin-top: 2rem;
    width: 50%;
  }

  @media(min-width: 769px){
    hr{
      display: block;
    }
  }
</style>
